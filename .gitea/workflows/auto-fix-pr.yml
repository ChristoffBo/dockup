name: AI Autofix (Gemini, PR-only)

on:
  push:
    branches:
      - main

jobs:
  ai-autofix:
    runs-on: ubuntu-latest
    # Required for pushing code and creating PRs
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Generate commit diff
        run: |
          git diff HEAD^ HEAD > changes.diff
          if [ ! -s changes.diff ]; then
            echo "No changes detected"
            exit 0
          fi

      - name: Build Gemini request
        run: |
          python3 << 'EOF'
          import json, textwrap

          try:
              with open("changes.diff", "r") as f:
                  diff = f.read()
          except FileNotFoundError:
              diff = ""

          prompt = textwrap.dedent("""\
          You are an automated code-fixing system.

          STRICT RULES:
          - Output ONLY a valid unified diff
          - NO markdown (do not use ```diff blocks)
          - NO explanations
          - NO commentary
          - NO surrounding text
          - If no fix is required, output exactly: NO_CHANGES

          Fix bugs, security issues, or correctness problems if present.

          Diff:
          """) + diff

          request = {
            "contents": [{
              "parts": [{"text": prompt}]
            }]
          }

          with open("request.json", "w") as f:
            json.dump(request, f)
          EOF

      - name: Call Gemini API
        run: |
          # Changed to gemini-2.0-flash (stable)
          curl -sS -X POST \
            "[https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=$](https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=$){{ secrets.GEMINI_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d @request.json > response.json

      - name: Extract and normalize unified diff
        run: |
          python3 << 'EOF'
          import json, sys

          try:
              with open("response.json") as f:
                  data = json.load(f)
              text = data["candidates"][0]["content"]["parts"][0]["text"]
          except Exception as e:
              print(f"Error parsing Gemini response: {e}")
              sys.exit(1)

          text = text.strip()

          if text == "NO_CHANGES" or not text:
              open("ai.patch", "w").write("")
              sys.exit(0)

          # Remove markdown code fences if Gemini ignores the prompt rules
          lines = text.splitlines()
          if lines[0].startswith("```"):
              lines = lines[1:]
          if lines and lines[-1].startswith("```"):
              lines = lines[:-1]

          start = None
          for i, line in enumerate(lines):
              if line.startswith("diff --git") or line.startswith("--- "):
                  start = i
                  break

          if start is None:
              print("No unified diff found in AI output")
              sys.exit(1)

          patch = "\n".join(lines[start:])
          with open("ai.patch", "w") as f:
              f.write(patch)
          EOF

      - name: Validate patch with git
        run: |
          if [ ! -s ai.patch ]; then
            echo "No AI changes to apply."
            exit 0
          fi
          git apply --check ai.patch

      - name: Apply patch and commit
        if: hashFiles('ai.patch') != ''
        run: |
          # Use GITEA_SHA if available, otherwise fallback to GITHUB_SHA
          SHA="${GITEA_SHA:-$GITHUB_SHA}"
          BRANCH="ai-fix-${SHA:0:7}"
          
          # Configure Git Identity
          git config --global user.name "AI Autofix Bot"
          git config --global user.email "ai-bot@noreply.gitea.io"

          git checkout -b "$BRANCH"
          git apply ai.patch
          git commit -am "AI autofix for ${SHA:0:7}"
          git push origin "$BRANCH"

          # Export branch for next step
          echo "AI_BRANCH=$BRANCH" >> $GITHUB_ENV

      - name: Create Gitea pull request
        if: env.AI_BRANCH != ''
        run: |
          # Detect Repo/Owner for Gitea/GitHub compatibility
          REPO_PATH="${GITEA_REPOSITORY:-$GITHUB_REPOSITORY}"
          OWNER=$(echo "$REPO_PATH" | cut -d/ -f1)
          REPO=$(echo "$REPO_PATH" | cut -d/ -f2)
          API_URL="${GITEA_URL:-$GITHUB_API_URL}"

          curl -sS -X POST \
            "${API_URL}/api/v1/repos/${OWNER}/${REPO}/pulls" \
            -H "Authorization: token ${{ secrets.GITEA_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d "{
              \"title\": \"AI Autofix\",
              \"head\": \"${AI_BRANCH}\",
              \"base\": \"main\",
              \"body\": \"Automated AI-generated fix for commit ${GITEA_SHA}. Please review carefully.\"
            }"

      - name: Notify Gotify
        if: always()
        run: |
          curl -X POST "${{ secrets.GOTIFY_URL }}/message?token=${{ secrets.GOTIFY_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d "{
              \"title\": \"AI Autofix Job Finished\",
              \"message\": \"Repo: ${GITEA_REPOSITORY:-$GITHUB_REPOSITORY}\nBranch: ${AI_BRANCH:-none}\",
              \"priority\": 5
            }"